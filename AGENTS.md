# AI Agent Guidelines (AGENTS.md)

이 문서는 **jabatta-html** 리포지토리에서 작업하는 모든 AI 에이전트(Antigravity, Claude Code, Cursor, Copilot 등)가 반드시 준수해야 하는 최상위 프로젝트 규칙입니다.

---

## 🏛️ 1. 프로젝트 원칙 및 제약사항

### 1. 단일 파일 완결성 (Single-File Principle)
- 이 프로젝트의 웹 애플리케이션은 **`index.html` 단 1개의 파일**로 배포 및 호스팅됩니다.
- 복잡한 빌드 파이프라인이나 번들러, 별도 `.css`, `.js` 외부 참조를 추가하지 마십시오.
- 모든 스타일과 스크립트는 `index.html` 내부에 인라인되어야 합니다.

### 2. 북마클릿 문법 엄격 준수 (Bookmarklet Constraints)
- 북마클릿 코드는 브라우저 북마크 URL 란(`javascript:...`)에 직접 삽입되므로 다음 제약을 엄격히 지켜야 합니다:
  1. **`#` 문자 절대 사용 금지**: URL fragment로 오인되어 코드가 잘립니다. 색상 표현 시 `#3b82f6` 대신 `rgb(...)` 형식을 사용하십시오.
  2. **URL 인코딩 안전성**: 줄바꿈이나 특수문자가 북마크바 드래그 시 깨지지 않도록 최소한의 인라인 함수 구조(`javascript:(function(){...})();`)를 유지하십시오.
  3. **스코프 오염 방지**: `window.__jabatta_*` 네임스페이스를 사용하여 호스트 페이지(코레일)의 기존 전역 변수와 충돌하지 않도록 하십시오.

---

## 🛡️ 2. 안정성 및 메모리 누수 방지 원칙

1. **`setInterval` 전면 금지**: 비동기 네트워크 통신 환경에서는 무조건 **재귀적 `setTimeout`**을 사용하십시오.
2. **`cache: 'no-store'` 의무화**: fetch 호출 시 브라우저 메인 프로세스에 캐시 메타데이터가 누적되어 브라우저 전체가 강제 종료(SIGTRAP, OOM)되는 것을 차단하십시오.
3. **루프 내 불필요한 객체 생성 금지**: 고정 파라미터는 시작 전 단 1회 문자열로 캐싱하십시오.

---

## 🔐 3. 보안 및 사용자 개인정보 보호

1. **비밀번호 저장 방식**:
   - SHA-256 해시 대조 방식으로만 인증을 수행하십시오.
   - 한글과 영문 두벌식 자판 상태를 모두 수용하는 `korToEng` 변환 알고리즘을 항상 유지하십시오.
2. **클라이언트 사이드 로컬 저장 (`localStorage`)**:
   - 사용자의 텔레그램 봇 토큰, 채팅 ID, 열차 설정은 어떠한 경우에도 외부 서버로 전송하지 마십시오. 오직 브라우저 내부 `localStorage`에만 저장 및 복원되어야 합니다.

---

## 🚀 4. 깃허브 배포 워크플로우

1. **GitHub Pages 브랜치**: `main` 브랜치 루트(`/`) 경로로 자동 배포됩니다.
2. **배포 검증**:
   - 커밋 후 `git push origin main` 실행
   - 배포 URL: `https://kimmjen.github.io/jabatta-html/`
   - 페이지 변경 후 시크릿 모드 또는 캐시 새로고침(`Cmd+Shift+R`)으로 반영 여부를 검증하십시오.
